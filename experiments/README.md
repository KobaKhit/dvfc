# Out-of-workspace experiments

Packages here are **not** listed in `pnpm-workspace.yaml`. They are kept for spikes and adapter prototypes only.

- `adapter-bruin/` — Bruin asset resolver experiment
- `adapter-sqlmesh/` — SQLMesh resolver experiment

Install and build them manually from this directory if you need to try them; they are not part of CI or the published dvfc stack.
