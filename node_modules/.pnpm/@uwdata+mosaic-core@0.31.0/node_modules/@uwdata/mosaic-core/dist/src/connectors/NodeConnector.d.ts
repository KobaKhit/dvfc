import type { ExtractionOptions, Table } from '@uwdata/flechette';
import { DuckDB } from '@uwdata/mosaic-duckdb';
import type { ArrowQueryRequest, Connector, ExecQueryRequest, JSONQueryRequest } from './Connector.js';
/**
 * A Mosaic Connector backed by an in-process Node.js DuckDB instance.
 * Requires the optional peer dependency `@uwdata/mosaic-duckdb`.
 */
export declare class NodeConnector implements Connector {
    protected _db: DuckDB;
    protected _ipc?: ExtractionOptions;
    static make(db?: DuckDB, ipc?: ExtractionOptions): Promise<NodeConnector>;
    constructor(db?: DuckDB, ipc?: ExtractionOptions);
    /**
     * Query an in-process DuckDB instance.
     * @param query Query object with type and SQL
     * @returns the query result
     */
    query(query: ArrowQueryRequest): Promise<Table>;
    query(query: ExecQueryRequest): Promise<void>;
    query(query: JSONQueryRequest): Promise<Record<string, unknown>[]>;
}
//# sourceMappingURL=NodeConnector.d.ts.map