import type { SampleClauseNode } from './sample.js';
import { SQLNode } from './node.js';
/**
 * AST node corresponding to a table source which can appear
 * within a SQL `FROM` clause.
 */
export declare class FromNode extends SQLNode {
}
export declare class FromClauseNode extends FromNode {
    /** The from expression. */
    readonly expr: SQLNode;
    /** The output name. */
    readonly alias?: string;
    /** The column names aliases. */
    readonly columnNames?: string[];
    /** The table sample. */
    readonly sample?: SampleClauseNode;
    /**
     * Instantiate a from clause node.
     * @param expr The from expression.
     * @param alias The output name.
     * @param sample The table sample.
     * @param columnNames The column names aliases (if any).
     */
    constructor(expr: SQLNode, alias?: string, sample?: SampleClauseNode, columnNames?: string[]);
    /**
     * Create a new from clause node that uses the given alias.
     * @param string alias
     */
    as(alias: string): FromClauseNode;
}
//# sourceMappingURL=from.d.ts.map