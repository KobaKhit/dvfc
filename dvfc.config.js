/**
 * Optional project config for dvfc.
 * Place at repo / project root as dvfc.config.js
 *
 * @type {import('@dvfc/core').DvfcConfig}
 */
export default {
  // chartTypes: ['./types/my-hexbin.js', '@acme/dvfc-sankey'],
};
